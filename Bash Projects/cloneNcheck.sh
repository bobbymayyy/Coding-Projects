#!/bin/bash
#This is a script to clone a removable SSD to an ISO, burn an ISO file to multiple physical drives at once, then hash and compare the mounted ISO with the built drives to test build viability.
clear

# Cursory ================
