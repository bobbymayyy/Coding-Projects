---
- name: Deployment of Security Onion cluster by template backup
  hosts: prox_master
  vars:
    int_list: "{{ p_vmbr_ints.split(' ') }}"
  tasks:
    - name: Get hostname for later use
      ansible.builtin.command: hostname
      register: proxHostname
    - name: Create vmbrs for ingestion
      ansible.builtin.command: pvesh create /nodes/"{{ proxHostname.stdout }}"/network --iface vmbr"{{ index+1 }}" --type OVSBridge --autostart true --ovs_ports "{{ item }}" --comments Ingestion
      loop: "{{ int_list }}"
      loop_control:
        index_var: index
    - name: Reload network configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/network
    - name: Restore SecO VM from backup template for Manager
      ansible.builtin.command: qmrestore --unique true /var/lib/vz/dump/vzdump-qemu-100-2023_11_27-12_16_57.vma.zst 110
      register: secOmanagerTemprestore
      ignore_errors: True
    - name: Print secOmanagerTemprestore
      ansible.builtin.debug: msg="{{ secOmanagerTemprestore.stdout_lines }}"
    - name: Restore SecO VM from backup template for Search
      ansible.builtin.command: qmrestore --unique true /var/lib/vz/dump/vzdump-qemu-100-2023_11_27-12_20_23.vma.zst 111
      register: secOsearchTemprestore
      ignore_errors: True
    - name: Print secOsearchTemprestore
      ansible.builtin.debug: msg="{{ secOsearchTemprestore.stdout_lines }}"
    - name: Restore SecO VM from backup template for Search 2
      ansible.builtin.command: qmrestore --unique true /var/lib/vz/dump/vzdump-qemu-100-2023_11_27-12_20_23.vma.zst 112
      register: secOsearch2Temprestore
      ignore_errors: True
    - name: Print secOsearch2Temprestore
      ansible.builtin.debug: msg="{{ secOsearch2Temprestore.stdout_lines }}"
    - name: Restore SecO VM from backup template for Forward
      ansible.builtin.command: qmrestore --unique true /var/lib/vz/dump/vzdump-qemu-100-2023_11_27-12_23_12.vma.zst 113
      register: secOforwardTemprestore
      ignore_errors: True
    - name: Print secOforwardTemprestore
      ansible.builtin.debug: msg="{{ secOforwardTemprestore.stdout_lines }}"
    - name: Restore SecO VM from backup template for Forward 2
      ansible.builtin.command: qmrestore --unique true /var/lib/vz/dump/vzdump-qemu-100-2023_11_27-12_23_12.vma.zst 114
      register: secOforward2Temprestore
      ignore_errors: True
    - name: Print secOforward2Temprestore
      ansible.builtin.debug: msg="{{ secOforward2Temprestore.stdout_lines }}"
    - name: Restore SecO VM from backup template for Forward 3
      ansible.builtin.command: qmrestore --unique true /var/lib/vz/dump/vzdump-qemu-100-2023_11_27-12_23_12.vma.zst 115
      register: secOforward3Temprestore
      ignore_errors: True
    - name: Print secOforward3Temprestore
      ansible.builtin.debug: msg="{{ secOforward3Temprestore.stdout_lines }}"
    - name: Restore SecO VM from backup template for Forward 4
      ansible.builtin.command: qmrestore --unique true /var/lib/vz/dump/vzdump-qemu-100-2023_11_27-12_23_12.vma.zst 116
      register: secOforward4Temprestore
      ignore_errors: True
    - name: Print secOforward4Temprestore
      ansible.builtin.debug: msg="{{ secOforward4Temprestore.stdout_lines }}"
    - name: Restore SecO VM from backup template for Forward 5
      ansible.builtin.command: qmrestore --unique true /var/lib/vz/dump/vzdump-qemu-100-2023_11_27-12_23_12.vma.zst 117
      register: secOforward5Temprestore
      ignore_errors: True
    - name: Print secOforward5Temprestore
      ansible.builtin.debug: msg="{{ secOforward5Temprestore.stdout_lines }}"
    - name: Pause for 45 seconds to wait for restore
      ansible.builtin.pause:
        seconds: 45
    - name: Resize the restored hard disk
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/resize --disk scsi0 --size 10000G
    - name: Resize the restored hard disk
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/config --net1 'model=virtio,bridge=vmbr2'
    - name: Change the bridge connected to the restored VM NIC
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/resize --disk scsi0 --size 10000G
    - name: Resize the restored hard disk
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/config --net1 'model=virtio,bridge=vmbr3'
    - name: Change the bridge connected to the restored VM NIC
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/resize --disk scsi0 --size 10000G
    - name: Resize the restored hard disk
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/config --net1 'model=virtio,bridge=vmbr4'
    - name: Change the bridge connected to the restored VM NIC
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/resize --disk scsi0 --size 10000G
    - name: Resize the restored hard disk
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/config --net1 'model=virtio,bridge=vmbr5'
    - name: Change the bridge connected to the restored VM NIC
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/resize --disk scsi0 --size 10000G
    - name: Start SecO Manager VM
      community.general.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: root@pam
        api_password: "{{ ansible_password }}"
        name: SecOManager
        vmid: 110
        node: "{{ proxHostname.stdout }}"
        state: started
      changed_when: true
    - name: Start SecO Search VM
      community.general.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: root@pam
        api_password: "{{ ansible_password }}"
        name: SecOSearch
        vmid: 111
        node: "{{ proxHostname.stdout }}"
        state: started
      changed_when: true
    - name: Start SecO Search 2 VM
      community.general.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: root@pam
        api_password: "{{ ansible_password }}"
        name: SecOSearch
        vmid: 112
        node: "{{ proxHostname.stdout }}"
        state: started
      changed_when: true
    - name: Start SecO Forward VM
      community.general.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: root@pam
        api_password: "{{ ansible_password }}"
        name: SecOForward
        vmid: 113
        node: "{{ proxHostname.stdout }}"
        state: started
      changed_when: true
    - name: Start SecO Forward 2 VM
      community.general.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: root@pam
        api_password: "{{ ansible_password }}"
        name: SecOForward2
        vmid: 114
        node: "{{ proxHostname.stdout }}"
        state: started
      changed_when: true
    - name: Start SecO Forward 3 VM
      community.general.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: root@pam
        api_password: "{{ ansible_password }}"
        name: SecOForward3
        vmid: 115
        node: "{{ proxHostname.stdout }}"
        state: started
      changed_when: true
    - name: Start SecO Forward 4 VM
      community.general.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: root@pam
        api_password: "{{ ansible_password }}"
        name: SecOForward4
        vmid: 116
        node: "{{ proxHostname.stdout }}"
        state: started
      changed_when: true
    - name: Start SecO Forward 5 VM
      community.general.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: root@pam
        api_password: "{{ ansible_password }}"
        name: SecOForward5
        vmid: 117
        node: "{{ proxHostname.stdout }}"
        state: started
      changed_when: true
    - name: Pause for 90 seconds to wait for boot
      ansible.builtin.pause:
        seconds: 90
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key y
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key s
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key f
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key n
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key r
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Pause for 480 seconds to wait for install
      ansible.builtin.pause:
        seconds: 480
    - name: Press enter to reboot
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Pause for 90 seconds to wait for boot
      ansible.builtin.pause:
        seconds: 90
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key f
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key n
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key r
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Select install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key down
    - name: Select distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Select new distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Select manager only setup
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Type agree for Elv2 license
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key a
    - name: Type agree for Elv2 license
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key g
    - name: Type agree for Elv2 license
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key r
    - name: Type agree for Elv2 license
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Type agree for Elv2 license
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Type agree for Elv2 license
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Move airgap installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key down
    - name: Select airgap installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Add -manager to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key minus
    - name: Add -manager to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key m
    - name: Add -manager to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key a
    - name: Add -manager to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key n
    - name: Add -manager to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key a
    - name: Add -manager to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key g
    - name: Add -manager to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Add -manager to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key r
    - name: Add -manager to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Skip a description
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Select vmbr0 as management NIC
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Setup static IP
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 0
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key dot
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 0
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key dot
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key dot
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 0
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key slash
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 2
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 4
    - name: Make Sec O Manager IP 10.10.11.110/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 2
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 5
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 4
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Skip DNS server configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key backspace
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key c
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key p
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key b
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key dot
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key m
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key i
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key l
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Pause for 15 seconds to wait for config changes
      ansible.builtin.pause:
        seconds: 15
    - name: Keep default docker range
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Pause for 15 seconds to wait for config changes
      ansible.builtin.pause:
        seconds: 15
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key d
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key f
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key n
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key d
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key e
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key r
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key c
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key p
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key b
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key dot
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key m
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key i
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key l
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Choose IP to access webgui
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Allow access to SecO install from webgui
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 0
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key dot
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 0
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key dot
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 1
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key dot
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 0
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key slash
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 2
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key 4
    - name: Enter analyst network for access to SecO
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Confirm configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key tab
    - name: Confirm configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/110/sendkey --key ret
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key y
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key e
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key s
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key f
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key n
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key r
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Pause for 480 seconds to wait for install
      ansible.builtin.pause:
        seconds: 480
    - name: Press enter to reboot
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Pause for 90 seconds to wait for boot
      ansible.builtin.pause:
        seconds: 90
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key f
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key n
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key r
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Select install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key down
    - name: Select distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key down
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Select search node deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key down
    - name: Select search node deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Add -search to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key minus
    - name: Add -search to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key s
    - name: Add -search to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key e
    - name: Add -search to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key a
    - name: Add -search to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key r
    - name: Add -search to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key c
    - name: Add -search to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key h
    - name: Add -search to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Skip a description
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Select vmbr0 as management NIC
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Setup static IP
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 0
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key dot
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 0
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key dot
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key dot
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key slash
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 2
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 4
    - name: Make Sec O Search IP 10.10.11.111/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 2
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 5
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 4
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Skip DNS server configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key backspace
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key c
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key p
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key b
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key dot
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key m
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key i
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key l
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Pause for 15 seconds to wait for config changes
      ansible.builtin.pause:
        seconds: 15
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key s
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key c
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key u
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key t
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key y
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key minus
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key m
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key g
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key 0
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key y
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key e
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key s
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key f
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key n
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key r
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Pause for 480 seconds to wait for install
      ansible.builtin.pause:
        seconds: 480
    - name: Press enter to reboot
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Pause for 90 seconds to wait for boot
      ansible.builtin.pause:
        seconds: 90
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key f
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key n
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key r
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Select install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key down
    - name: Select distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key down
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Select search node deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key down
    - name: Select search node deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Add -search2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key minus
    - name: Add -search2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key s
    - name: Add -search2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key e
    - name: Add -search2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key a
    - name: Add -search2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key r
    - name: Add -search2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key c
    - name: Add -search2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key h
    - name: Add -search2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 2
    - name: Add -search2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Skip a description
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Select vmbr0 as management NIC
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Setup static IP
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 0
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key dot
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 0
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key dot
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key dot
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 2
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key slash
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 2
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 4
    - name: Make Sec O Search2 IP 10.10.11.112/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 2
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 5
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 4
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Skip DNS server configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key backspace
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key c
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key p
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key b
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key dot
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key m
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key i
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key l
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Pause for 15 seconds to wait for config changes
      ansible.builtin.pause:
        seconds: 15
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key s
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key c
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key u
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key t
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key y
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key minus
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key m
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key g
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key 0
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key y
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key e
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key s
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key f
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key n
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key r
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Pause for 480 seconds to wait for install
      ansible.builtin.pause:
        seconds: 480
    - name: Press enter to reboot
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Pause for 90 seconds to wait for boot
      ansible.builtin.pause:
        seconds: 90
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key f
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key n
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key r
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Select install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key down
    - name: Select distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key down
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Select sensor deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Add -sensor to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key minus
    - name: Add -sensor to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key s
    - name: Add -sensor to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key e
    - name: Add -sensor to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key n
    - name: Add -sensor to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key s
    - name: Add -sensor to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key o
    - name: Add -sensor to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key r
    - name: Add -sensor to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Skip a description
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Select vmbr0 as management NIC
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Setup static IP
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 0
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 0
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 3
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key slash
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 2
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 4
    - name: Make Sec O Sensor IP 10.10.11.113/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 2
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 5
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 4
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Skip DNS server configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key backspace
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key c
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key p
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key b
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key dot
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key m
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key i
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key l
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Pause for 15 seconds to wait for config changes
      ansible.builtin.pause:
        seconds: 15
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key s
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key c
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key u
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key t
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key y
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key minus
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key m
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key g
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key 0
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key y
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key e
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key s
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key f
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key n
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key r
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Pause for 480 seconds to wait for install
      ansible.builtin.pause:
        seconds: 480
    - name: Press enter to reboot
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Pause for 90 seconds to wait for boot
      ansible.builtin.pause:
        seconds: 90
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key f
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key n
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key r
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Select install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key down
    - name: Select distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key down
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Select sensor deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Add -sensor2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key minus
    - name: Add -sensor2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key s
    - name: Add -sensor2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key e
    - name: Add -sensor2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key n
    - name: Add -sensor2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key s
    - name: Add -sensor2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key o
    - name: Add -sensor2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key r
    - name: Add -sensor2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 2
    - name: Add -sensor2 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Skip a description
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Select vmbr0 as management NIC
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Setup static IP
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 0
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 0
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 4
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key slash
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 2
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 4
    - name: Make Sec O Sensor IP 10.10.11.114/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 2
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 5
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 4
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Skip DNS server configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key backspace
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key c
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key p
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key b
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key dot
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key m
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key i
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key l
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Pause for 15 seconds to wait for config changes
      ansible.builtin.pause:
        seconds: 15
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key s
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key c
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key u
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key t
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key y
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key minus
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key m
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key g
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key 0
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key y
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key e
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key s
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key f
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key n
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key r
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Pause for 480 seconds to wait for install
      ansible.builtin.pause:
        seconds: 480
    - name: Press enter to reboot
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Pause for 90 seconds to wait for boot
      ansible.builtin.pause:
        seconds: 90
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key f
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key n
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key r
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Select install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key down
    - name: Select distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key down
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Select sensor deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Add -sensor3 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key minus
    - name: Add -sensor3 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key s
    - name: Add -sensor3 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key e
    - name: Add -sensor3 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key n
    - name: Add -sensor3 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key s
    - name: Add -sensor3 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key o
    - name: Add -sensor3 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key r
    - name: Add -sensor3 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 3
    - name: Add -sensor3 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Skip a description
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Select vmbr0 as management NIC
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Setup static IP
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 0
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 0
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 5
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key slash
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 2
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 4
    - name: Make Sec O Sensor IP 10.10.11.115/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 2
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 5
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 4
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Skip DNS server configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key backspace
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key c
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key p
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key b
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key dot
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key m
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key i
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key l
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Pause for 15 seconds to wait for config changes
      ansible.builtin.pause:
        seconds: 15
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key s
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key c
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key u
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key t
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key y
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key minus
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key m
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key g
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key 0
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key y
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key e
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key s
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key f
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key n
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key r
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Pause for 480 seconds to wait for install
      ansible.builtin.pause:
        seconds: 480
    - name: Press enter to reboot
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Pause for 90 seconds to wait for boot
      ansible.builtin.pause:
        seconds: 90
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key f
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key n
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key r
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Select install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key down
    - name: Select distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key down
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Select sensor deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Add -sensor4 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key minus
    - name: Add -sensor4 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key s
    - name: Add -sensor4 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key e
    - name: Add -sensor4 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key n
    - name: Add -sensor4 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key s
    - name: Add -sensor4 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key o
    - name: Add -sensor4 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key r
    - name: Add -sensor4 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 4
    - name: Add -sensor4 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Skip a description
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Select vmbr0 as management NIC
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Setup static IP
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 0
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 0
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 6
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key slash
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 2
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 4
    - name: Make Sec O Sensor IP 10.10.11.116/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 2
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 5
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 4
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Skip DNS server configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key backspace
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key c
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key p
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key b
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key dot
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key m
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key i
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key l
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Pause for 15 seconds to wait for config changes
      ansible.builtin.pause:
        seconds: 15
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key s
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key c
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key u
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key t
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key y
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key minus
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key m
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key g
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key 0
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key y
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key e
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key s
    - name: Type yes to confirm disk destruction
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key f
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key n
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key d
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key e
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key r
    - name: Create a user for install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Pause for 480 seconds to wait for install
      ansible.builtin.pause:
        seconds: 480
    - name: Press enter to reboot
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Pause for 90 seconds to wait for boot
      ansible.builtin.pause:
        seconds: 90
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key f
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key n
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key d
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key e
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key r
    - name: Login to start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-1
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-q
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-a
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-z
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-2
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-w
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-s
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key shift-x
    - name: Create admin account for login
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Start configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Select install
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key down
    - name: Move to distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key down
    - name: Select distributed installation
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key down
    - name: Select existing distributed deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Select sensor deployment
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Add -sensor5 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key minus
    - name: Add -sensor5 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key s
    - name: Add -sensor5 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key e
    - name: Add -sensor5 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key n
    - name: Add -sensor5 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key s
    - name: Add -sensor5 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key o
    - name: Add -sensor5 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key r
    - name: Add -sensor5 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 5
    - name: Add -sensor5 to end of securityonion
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Skip a description
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Select vmbr0 as management NIC
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Setup static IP
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 0
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 0
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key dot
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 7
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key slash
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 2
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 4
    - name: Make Sec O Sensor IP 10.10.11.117/24
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 0
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key dot
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 2
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 5
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 4
    - name: Make the gateway IP 10.10.11.254
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Skip DNS server configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Backspace to add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key backspace
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key c
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key p
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key b
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key dot
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key m
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key i
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key l
    - name: Add search domain
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Pause for 15 seconds to wait for config changes
      ansible.builtin.pause:
        seconds: 15
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key s
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key c
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key u
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key t
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key y
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key i
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key o
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key minus
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key m
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key n
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key a
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key g
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key e
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key r
    - name: Add manager's hostname (case sensitive)
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key dot
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 1
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key 0
    - name: Pause for 2400 seconds to wait for install (40 min)
      ansible.builtin.pause:
        seconds: 2400
    - name: Add IP to vmbr0 to access Sec O Manager
      ansible.builtin.command: ip a add 10.10.11.5/24 dev vmbr0
    - name: SSH into Manager to add IP to search role
      ansible.builtin.command: sshpass -p "{{ ansible_password }}" ssh -o StrictHostKeyChecking=no defender@10.10.11.110 'echo 1qaz2wsx!QAZ@WSX | sudo -S so-firewall-minion --role=SEARCHNODE --ip=10.10.11.111'
    - name: SSH into Manager to add IP to search role
      ansible.builtin.command: sshpass -p "{{ ansible_password }}" ssh -o StrictHostKeyChecking=no defender@10.10.11.110 'echo 1qaz2wsx!QAZ@WSX | sudo -S so-firewall-minion --role=SEARCHNODE --ip=10.10.11.112'
    - name: SSH into Manager to add IP to search role
      ansible.builtin.command: sshpass -p "{{ ansible_password }}" ssh -o StrictHostKeyChecking=no defender@10.10.11.110 'echo 1qaz2wsx!QAZ@WSX | sudo -S so-firewall-minion --role=SENSOR --ip=10.10.11.113'
    - name: SSH into Manager to add IP to search role
      ansible.builtin.command: sshpass -p "{{ ansible_password }}" ssh -o StrictHostKeyChecking=no defender@10.10.11.110 'echo 1qaz2wsx!QAZ@WSX | sudo -S so-firewall-minion --role=SENSOR --ip=10.10.11.114'
    - name: SSH into Manager to add IP to search role
      ansible.builtin.command: sshpass -p "{{ ansible_password }}" ssh -o StrictHostKeyChecking=no defender@10.10.11.110 'echo 1qaz2wsx!QAZ@WSX | sudo -S so-firewall-minion --role=SENSOR --ip=10.10.11.115'
    - name: SSH into Manager to add IP to search role
      ansible.builtin.command: sshpass -p "{{ ansible_password }}" ssh -o StrictHostKeyChecking=no defender@10.10.11.110 'echo 1qaz2wsx!QAZ@WSX | sudo -S so-firewall-minion --role=SENSOR --ip=10.10.11.116'
    - name: SSH into Manager to add IP to search role
      ansible.builtin.command: sshpass -p "{{ ansible_password }}" ssh -o StrictHostKeyChecking=no defender@10.10.11.110 'echo 1qaz2wsx!QAZ@WSX | sudo -S so-firewall-minion --role=SENSOR --ip=10.10.11.117'
    - name: Add IP to vmbr0 to access Sec O Manager
      ansible.builtin.command: ip a del 10.10.11.5/24 dev vmbr0
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Pause for 10 seconds to check for connectivity
      ansible.builtin.pause:
        seconds: 10
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Pause for 10 seconds to check for connectivity
      ansible.builtin.pause:
        seconds: 10
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Pause for 10 seconds to check for connectivity
      ansible.builtin.pause:
        seconds: 10
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Pause for 10 seconds to check for connectivity
      ansible.builtin.pause:
        seconds: 10
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Pause for 10 seconds to check for connectivity
      ansible.builtin.pause:
        seconds: 10
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Pause for 10 seconds to check for connectivity
      ansible.builtin.pause:
        seconds: 10
    - name: Add manager's IP address
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Pause for 10 seconds to check for connectivity
      ansible.builtin.pause:
        seconds: 10
    - name: Confirm
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key tab
    - name: Confirm
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/111/sendkey --key ret
    - name: Confirm
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key tab
    - name: Confirm
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/112/sendkey --key ret
    - name: Set monitor interface
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key spc
    - name: Set monitor interface
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Confirm configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key tab
    - name: Confirm configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/113/sendkey --key ret
    - name: Set monitor interface
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key spc
    - name: Set monitor interface
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Confirm configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key tab
    - name: Confirm configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/114/sendkey --key ret
    - name: Set monitor interface
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key spc
    - name: Set monitor interface
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Confirm configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key tab
    - name: Confirm configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/115/sendkey --key ret
    - name: Set monitor interface
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key spc
    - name: Set monitor interface
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Confirm configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key tab
    - name: Confirm configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/116/sendkey --key ret
    - name: Set monitor interface
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key spc
    - name: Set monitor interface
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    - name: Confirm configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key tab
    - name: Confirm configuration
      ansible.builtin.command: pvesh set /nodes/"{{ proxHostname.stdout }}"/qemu/117/sendkey --key ret
    
